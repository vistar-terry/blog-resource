interface:
  display_name: "Blog Resource"
  short_description: "Repo-specific patterns and workflows for blog-resource"
  default_prompt: "Use the blog-resource repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true